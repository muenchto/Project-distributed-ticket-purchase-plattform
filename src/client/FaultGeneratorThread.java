
public FaultGeneratorThread() extends Thread{

    String type;
    String serverId;

    public FaultGeneratorThread(String type, String serverIdi, Remote stub){
        this.type = type;
        this.serverId = serverId;
        if(stub.getClass().equals)
    }

    @Override
    public void run(){
           
    }

}
